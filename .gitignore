# Build output. The leading slash matters: an unanchored "notion-track" would
# also match the cmd/notion-track/ source directory.
/notion-track
/dist/

# Go test artifacts
*.test
*.out

# Local agent workspace and notes, not published
.superpowers/
CLAUDE.md
